# Run full CLI

Can you:

1. Use the CLI
2. Create a new full patient repository using the `initialise_full_record` method in `crates/core/src/patient.rs`
3. Create a new letter with only a body
4. Create a new letter, but only with an attachment and no body
5. Create a new letter with both a body and an attachment
6. Create a new thread and add a few messages to it
